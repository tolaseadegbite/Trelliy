<%= turbo_stream.remove [@follow_up_task, @pending_follow_up] %>

<%#= turbo_stream.remove "modal" %>

<%= turbo_stream.prepend "flash", partial: "shared/flash", locals: { notice: "Follow-up successfully logged!" } %>
